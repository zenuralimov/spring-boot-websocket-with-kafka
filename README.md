Project String Boot WebSocket with Kafka
========================================
To start project run in root directory:
1. `docker-compose up -d`
2. `mvn spring-boot:run`
---------------------------------------
Server start on: `http://localhost:8080`